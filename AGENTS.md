# AGENTS.md

Инструкции для агентов, работающих над Tentex. Этот файл — единственный источник правды; `CLAUDE.md` его импортирует.

## Что это

Tentex — локальный тренажёр подготовки к экзамену. Курсовая по ТРПС, МГТУ им. Баумана, защита в декабре 2026. Один пользователь, одна установка, всё работает офлайн; внешние модели опциональны и всегда выключаемы.

Отличие от аналогов в одной фразе: остальные идут от документа к карточкам, Tentex идёт от цели обучения к программе и к **измеримому покрытию в обе стороны** — показывает и чего у тебя нет, и что ты собирался пропустить.

Подробности лежат в основных документах, и переписывать их в код не надо:

| Файл | Что внутри | Когда открывать |
| --- | --- | --- |
| `BRIEF.md` | Рассказ о продукте, зачем всё это | Не понимаешь смысл функции |
| `REQUIREMENTS.md`(Не опираться как на истину незыблемую во время разработки экранов, будет переписыватся еще, смотрим тут функционал который хотели заложить на стадии планирования) | Требования: FR-*, NFR-*, §20 модель данных, §21 экраны, §23 стек | Вопрос «как система должна себя вести» |
| `PLAN.md` | Порядок работ по этапам и что уходит в записку | Вопрос «что делаем сейчас и что после» |
| `SCREENS.md` | Описания экранов: что на экране, действия, состояния, данные, переходы | Верстаешь экран или ищешь, какие ему нужны данные |
| `DESIGN.md` | Дизайн-система: слои, токены, компоненты, запреты по цвету | Пишешь компонент или выбираешь цвет |
| `docs/architecture/stage-2-core.md` | Реализованное ядро данных, API и аргументы для защиты | Меняешь сущности этапа 2 или готовишься объяснять архитектуру |

Перед тем как что-то придумывать — проверь, нет ли ответа в `REQUIREMENTS.md`. Обычно есть.

## Где сейчас находимся

**Этап 0 завершён:** монорепо поднято, темы работают, UI-кит переписан под свои дизайн-принципы. Заглушки экранов снесены: строить поверх чужого каркаса смысла нет.

**Этап 2 завершён:** SQLite работает в WAL через миграции; реализованы `Project`, `WizardDraft`, `GoalPassport`, `Material`, `ProjectMaterial`, `ProgramNode` и `WorkspaceState`, а также API для мастера, проектов, программы и раскладки. Черновик переживает перезапуск; паспорт цели обязателен. С этапа 5 экзаменационный и учебниковый проекты можно активировать с пустой программой и заполнить её позже в отдельном разделе. Сквозная проверка: `python backend/scripts/check_stage2.py`.

**Этап 3 завершён:** обе ветки мастера, Проекты, Программа и базовая Рабочая область работают через API; добавлены `Project.program_revision`, общий `ProjectActionLog`, импорт экзаменационного текста, undo и идемпотентный пример в SQLite. Проверки: `python backend/scripts/check_stage2.py` и `python backend/scripts/check_stage3.py`. Фактический контракт: `docs/architecture/stage-3-live-projects.md`.

**Этап 4 завершён:** эталонные ответы связаны со стабильными `ProgramNode.id`; есть детерминированный импорт без ИИ, ревизии, подтверждение, мягкое удаление, Карта эталонов и чтение в Рабочей области. Проект можно удалить полностью. Проверка: `python backend/scripts/check_stage4.py`. Фактический контракт: `docs/architecture/stage-4-reference-answers.md`.

**Этап 5B (экзаменационная итерация) завершён:** загрузка и разбор файлов, страницы, фрагменты, блоки, качество текста, Просмотрщик и возобновляемая фоновая обработка работают; фактический контракт — `docs/architecture/stage-5-material-pipeline.md`.

**Багфикс-вертикаль Материалов завершена (12.08.2026):** правая панель привязок перестроена, в просмотрщике есть листание по краям, полный экран и масштаб «вписать страницу», разбор сохраняет оформление PDF и Word (абзацы, списки, номера пунктов Word, картинки), поиск не лезет в файл вопросов, файл эталонных ответов у проекта один и после разбора сам ложится на вопросы по заголовкам и заполняет эталоны, картинки из материалов привязываются как обычные фрагменты, к эталону можно прикрепить свои файлы. Фактический контракт — `docs/architecture/materials-viewer-and-answers-autolink.md`. Осталось из этой вертикали: «Настройки программы у проекта» (недостающий экран этапа 1).

**Подготовительная вертикаль перед этапом 6, часть «поиск и ручная привязка» завершена (11.08.2026):** лексический поиск FTS5/BM25 с русской лемматизацией (pymorphy3) и ручная привязка фрагмента или блока к теме (сущность `Binding`, снятие/восстановление, undo через общий журнал, перенос при коррекции текста страницы, предупреждение при удалении материала) работают для экзаменационного проекта; фактический контракт — `docs/architecture/pre-stage-6-search-and-manual-binding.md`, сквозная проверка — `python backend/scripts/check_manual_binding.py`. Учебниковый раздел «Привязки» — эскиз в `SCREENS.md`, реализация после этапа 7. Не добавлять эмбеддинги, проход 2, машинные привязки, карточки, занятия или План раньше их этапов. Ветка «Свободное изучение» отложена до конца этапа 7.

**Идёт вертикаль «Экзамен с ИИ» (с 12.08.2026):** работаем по одному режиму за раз — сначала экзаменационный доводится до ежедневного пользования, учебниковый и всё, что его обслуживает, после. Заголовки, шлюз моделей, живые Параметры → ИИ, cleanup страницы и группировка плоской экзаменационной Программы **готовы 12.08.2026**. Первая итерация экзаменационного чата тоже **готова 12.08.2026**: потоковые ответы, письменные Попытки и Оценки, лесенка `точное совпадение → ключевые термины → ИИ-судья`, проверенные цитаты, кэш, самооценка и история работают вместе; контракт — `docs/architecture/exam-chat.md`, проверка — `python backend/scripts/check_exam_chat.py`. Перед итерацией 2 чат используют несколько дней на настоящем проекте и только затем уточняют доводку по наблюдениям. Эмбеддинги (этап 6), проход 1 и 2, режимы OCR сверх «Быстро», Студия и учебниковый режим **отложены, а не отменены**; поимённый список — в `PLAN.md`.

**Глобальная рабочая область материала завершена (14.08.2026):** Библиотека стала местом, где готовят общий источник — `/library/:materialId` открывается полноразмерной оболочкой с оглавлением, сравнением исходника и подготовленного текста и инспектором «Обработка · Версии · Файл». Материал создаётся без проекта и подключается к нему отдельным действием; появился реестр версий `MaterialRevision`, частичный повтор разбора собирает полную новую ревизию, прежние версии читаются и восстанавливаются как новые. Привязки остались проектными. Фактический контракт — `docs/architecture/global-library-material-workspace.md`, сквозная проверка — `python backend/scripts/check_library_workspace.py`.

**Дизайн-система собрана и описана в `DESIGN.md`:** три слоя (токены → примитивы → доменные виджеты), поведение примитивов взято у Radix. Прежде чем писать компонент — смотри витрину `/ui-kit` и скилл `tentex-ui`.

## Структура

```
backend/          FastAPI. app/main.py — приложение, app/config.py — настройки
frontend/         Vite + React + TypeScript
  src/app/        screens.ts (реестр), views.ts (что сверстано), AppLayout, CommandPalette, ThemeToggle
  src/screens/    По файлу или каталогу на свёрстанную поверхность
  src/components/ ui/ — примитивы; domain/ — доменные виджеты Tentex
  src/hooks/      useTheme
  src/styles/     tokens · base · ui-kit · domain · layout
.agents/skills/   Скиллы проекта
data/             SQLite и файловое хранилище. В git не едет
virtex/           Прошлый проект, только для чтения. В git не едет
```

**`frontend/src/app/screens.ts` — единственный список экранов.** Пятнадцать проектных из §21, два глобальных (Библиотека, Параметры) и витрина: там видно и что сделано, и что предстоит.

**`frontend/src/app/views.ts` — что из этого списка уже сверстано.** Роутинг и навигация строятся отсюда, поэтому ссылок в никуда не бывает. Спроектировал экран — добавил строку в `views.ts` и файл в `src/screens/`.

## Команды

Из корня:

```bash
npm run dev
```

Поднимает FastAPI на 8000 и Vite на 5173. Либо `docker compose up` — то же самое в контейнерах.

После любых правок фронтенда сразу перезапускай контейнер `web` командой `docker compose restart web`: на `http://localhost:5173` горячее обновление может не сработать и остаться старая сборка. Не считай изменение проверенным в браузере до перезапуска.

Не смотри в `data/tentex.sqlite` напрямую с хоста (`sqlite3`, DB-браузеры), пока контейнер `api` работает: база в режиме WAL, файл на bind-mount с Windows читается мимо WAL-журнала и отдаёт устаревший снимок. За текущим состоянием — через API или `docker compose exec api sqlite3 /data/tentex.sqlite`.

```bash
npm run typecheck
npm run build
npm run lint
```

Только фронт: `cd frontend && npm run dev | typecheck | build`.
Только бэк: `cd backend && python -m ruff check .`.

Тестового фреймворка и команды `npm test` пока нет. Для ядра этапа 2 есть сквозная проверка `python backend/scripts/check_stage2.py`; оснастка для точечных тестов вернётся на этапе 5 вместе с первой подсистемой, которую есть смысл тестировать.

## Стек

React + TypeScript + Vite · Radix Primitives для поведения компонентов · Python + FastAPI · SQLite с WAL везде, включая учебные базы · `sqlite-vec` для векторов · FTS5 для BM25 · PyMuPDF · PP-OCRv5 для русского / PaddleOCR-VL (этап 5) · pymorphy3 · SM-2 · aiogram · pytest + Vitest + Playwright.

Radix (пакет `radix-ui`) добавлен на этапе 1: всплывашки, диалоги, тумблеры и радиогруппы требуют фокус-ловушки, позиционирования с обходом краёв экрана и ARIA — это ровно тот случай, когда берётся готовое. Он отдаёт разметку без стилей, поэтому обычный CSS на токенах остаётся. Обоснование и что осталось своим — `DESIGN.md`.

Обоснование каждого выбора — §23 требований. Не меняй решение из этой таблицы, не сказав об этом: почти каждое защищается у доски.

Тяжёлые вещи приезжают на своих этапах, а не сейчас: OCR — этап 5, эмбеддинги — этап 6, шлюз моделей — этап 7.

Python: целевая версия **3.13** (так в контейнере). Локально стоит 3.14 — для этапов 0–4 разницы нет, но к этапу 6 понадобится PyTorch под CUDA 12.8, а он отстаёт от свежих версий Python.

Серверная часть virtex не переносится вообще — там Node/TS, у нас Python. Из virtex взят только клиентский UI-кит.

## Правила работы

**Готовое вместо своего.** Прежде чем писать механизм — проверь, есть ли библиотека или опубликованный алгоритм. Обоснование выбора ищется в литературе, а не в собственных замерах. При равном результате выбирается вариант, который рисуется блок-схемой: её потом надо защищать.

**Не разрастаться.** Проект пишется одним человеком за 10–15 часов в неделю. Всё, что нельзя объяснить за две минуты, — обуза. Не предлагай исследований и замеров, если о них не просили.

**Новая функция с внешним ИИ.** В той же задаче она обязана зарегистрировать или явно переиспользовать роль шлюза, указать модальность и требуемые возможности модели, появиться в разделе «ИИ» глобальных Параметров с наследованием общей модели текста или речи и отдельным переопределением, если оно нужно, а также определить версию промпта, значимые параметры, предпросмотр отправляемого, кэш, журнал токенов/стоимости и поведение при выключенном шлюзе или недоступном провайдере. Нельзя зашивать модель, `base_url`, ключ или расчёт стоимости внутрь самой функции.

**Рабочее дерево и коммиты.** Завершив самостоятельную, проверенную задачу, агент обязан сразу сделать отдельный коммит со всеми своими изменениями — до финального ответа. Уже грязное рабочее дерево не освобождает от этого: в коммит попадают только собственные файлы и свои строки, чужие изменения не добавляются и не исправляются. Если свои изменения невозможно отделить от чужих без риска, агент сообщает об этом до завершения задачи и просит указаний; молча оставлять готовую работу незафиксированной нельзя.

**Этап заканчивается тем, что можно показать.** Если у куска работы нет демонстрируемого результата — он нарезан неправильно.

**Открытый вопрос закрывается там, где он впервые мешает, и не раньше.** Пять открытых вопросов §27 уже расписаны по этапам в `PLAN.md`.

**Схема рисуется тогда, когда делается подсистема.** Час сейчас против дня в декабре — и та, что восстановлена по коду, содержит враньё. Что куда уходит — таблица в конце `PLAN.md`.

**Разведка не превращается в продакшн.** Скрипт разведки главной идеи выбрасывается, а не дорастает до этапа 8.

**Визуальный baseline этапа 1.** При оживлении уже свёрстанного экрана визуальная структура, порядок блоков и основные взаимодействия берутся из тега `ui-stage-1-baseline`. Отсутствующий API не является причиной менять компоновку: элемент остаётся на прежнем месте как честная заглушка. Новая структура допустима только после явного согласования с пользователем и записи причины в `docs/ui/stage-1-interface-baseline.md`.

## Как писать

**Код.** Русские тексты интерфейса, английские идентификаторы. Комментарий объясняет почему, а не что. Цвет — только через токен из `tokens.css`. Компонент — сначала посмотри в кит (скилл `tentex-ui`).

**Ответы и документы.** Конкретно, без промежуточных абстракций. Предлагаешь формат — сразу давай один заполненный пример, а не описание формата. Пункт плана должен выполняться без вопроса «а что это значит». Абстракция допустима только после конкретного случая.

**Тесты** пишутся там, где поведение легко ломается: разбиение на блоки (этап 5), интервалы SM-2 и нормализация ответов (этап 9), сравнение результатов SQL (этап 10). Сплошное покрытие не нужно. TDD в проекте выключен сознательно.

## Словарь

Термины из §20 требований. Пользуйся ими, не изобретай синонимов.

| Термин | Что это |
| --- | --- |
| **Узел программы** | Строка программы: раздел, тема или подпункт. Дерево до 4 уровней |
| **Тема** | Узел программы, по которому занимаются. Центральное понятие продукта |
| **Материал** | Загруженный файл: PDF, DOCX, TXT, MD, изображение |
| **Страница** | Страница материала с текстом и флагом качества: `native` · `ocr` · `ocr_low` |
| **Фрагмент** | Кусок текста страницы с координатами. Единица привязки и поиска |
| **Блок** | Смысловой кусок материала по заголовку. Бывает содержательный и служебный |
| **Привязка** | Связь темы и фрагмента. Статус: машинная · подтверждена · снята · ручная |
| **Проход 1** | Материал → программа. Map-reduce по главам |
| **Проход 2** | Полный перебор блоков: к какой теме относится каждый. Главный алгоритм проекта (А2) |
| **Покрытие в обе стороны** | Две метрики: сколько программы обеспечено материалом и сколько материала разнесено по программе |
| **Пробелы** | Темы без материала |
| **Неразобранное** | Блоки материала, не попавшие ни в одну тему |
| **Эталонный ответ** | Образцовый ответ по теме, с которым сравнивается твой |
| **Сила свидетельства** | Насколько активность подтверждает знание темы. Влияет на статус и на вес в повторениях |

## Границы

Не входит в продукт принципиально: чат с документом как главный сценарий · универсальная база знаний · платформа курсов · соцсеть · coding-агент · мобильное приложение · многопользовательский режим · оценка без объяснения.

Отложено осознанно, полный список — §22 требований: обучаемая модель памяти, обучаемый переранжировщик привязок, распознавание рукописных формул, PostgreSQL, EPUB и прочее. Если кажется, что чего-то не хватает — сначала проверь §22: скорее всего оно выкинуто намеренно и с причиной.

Линия отсечения на случай нехватки времени — в конце `PLAN.md`. Этапы 0–9 обязательны, остальное режется в описанном порядке.

**FR-D17, отдельно:** интерфейс **не должен** показывать покрытие материала ниже 100% как проблему. Это результат работы, а не дефект.

## Скиллы
Лежат в `.agents/skills/`.

Проектные: `skill-router` (выбор остальных) · `tentex-ui` (экраны, компоненты, стили) · `tentex-api` (бэкенд).
