# Tentex — рабочие задачи

> Срез 09.09.2026 по коду commit `9acd0f7`. Тесты, браузер и пользовательские файлы заново не запускались. Статус «открыто» не означает воспроизведённый дефект; «подтверждено кодом» не означает исправление.

## Активные задачи

| ID | Задача | Статус |
|---|---|---|
| UX-01 | После создания проекта различать реальный сбой запуска обработки и дублирующее предупреждение; сохранить понятный переход в Материалы | Открыто: текст предупреждения формируется в коде |
| UX-02 | Убрать недостоверную ETA локального разбора либо явно пометить её как грубую; стоимость cloud не скрывать | Нужно решение |
| OCR-01 | При «требует сверки» показывать причину: формула, схема, структура или непрочитанная область; не выдавать confidence за точность страницы | Открыто: причина расхождения видна статически |
| SET-01 | Уточнить, что требуется сохранять: последнюю позицию экрана или журнал конкретных изменений настроек | Нужна формулировка |
| IMP-01 | Воспроизвести раскладку секций файла ответов с ИИ на конкретных файлах: нумерация, границы, preview/apply, retry и идемпотентность | Открыто на воспроизведение |
| UX-03 | Перенести ошибку сохранения мастера к активной кнопке, сохранив alert и управление фокусом | Открыто: верхнее размещение видно в коде |
| IMP-02 | Свести сообщения о пропущенных строках в одну сводку с причиной и диапазоном | Нужен входной файл/DOM; одновременный дубль статически не доказан |
| ANS-01 | При удалении фото удалить связанный inline-marker из текста ответа и проверить сохранённый/несохранённый сценарий | Подтверждено кодом, не исправлено |
| SET-02 | Показывать независимый статус сохранения модели текста и речи рядом с каждой настройкой | Открыто: сейчас общий статус |
| ARCH-01 | Разделить исследование качества: циклические зависимости, границы модулей/API, крупные screen-компоненты, ответственность `screens.ts`/`views.ts`, критерии защиты и стандарт документации функций | Исследование, список дефектов ещё не подтверждён |
| NEXT-01 | Согласовать textbook/free + hybrid lexical/vector + оба направления привязки; не выбирать модель и vector engine до сравнения | Предложено, реализация не начата |

## Импортированный список пользователя — история, дословно

Ниже сохранены все исходные строки без закрытия и нормализации; рабочий статус задаёт таблица выше.

- Убрать: Проект создан, но не удалось запустить обработку 2 материалов. Продолжите её в разделе «Материалы».
- УБрать оценку времени при разрборе, пишет вообще неверно
- Страница 20 требует сверки с оригиналом. Уверенность распознавания — 100%.
- Жкран настроек:сохранять в соответскии с api дейтвия посление а не рпосто раздел, нужны конкретные изменения там
- Починить раскладку вопросов с ии из файла ответов, проблем много щас на всех этапах

- В мастере создания проекта ошибку о том, что нужно добавить еще какой-то материал (список вопросов, задач, учебные материалы), нужно выводить снизу, прямо над кнопкой «Сохранить», а не сверху над всем. Сейчас её просто не видно, и совершенно не понимаешь, почему проект не сохраняется и нельзя продолжить.

  Также нужно разобраться, что за бред происходит в предварительном разборе:

  1. Зачем-то два раза пишется, сколько строк пропущено перед списком.
  2. Затем пишется, сколько строк пропущено после списка.

  Что это вообще значит и зачем это нужно? Может, лучше это просто убрать?


- В разделе ответы когда убираешь вставленное фото у тебя из текста ответа не удаляется вставка сама

- При сохр модели по умолчанию для текста/речи надо сделать Сохранено снизу от каждого а не общее внизу и заметнее сделать


- Начать править архитектурные проблемы, по типу циклических зависимостей, поведение живет вне класса и тд. Если будет много токенов провести аудит нормальный, как доделаю режим учебника хотя бы. К каждой функции сделать документиацию. Модульные границы проверить и все что является критерием оценки на защите и тд. Еще про фронту: огромные screen компоненты, два реестра экранов(screens ts views ts)...