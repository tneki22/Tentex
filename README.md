# Tentex

Локальный тренажёр подготовки к экзамену. Курсовая по ТРПС, МГТУ им. Баумана.

Загружаешь учебные материалы, получаешь программу курса, и система показывает **покрытие в обе стороны**: чего у тебя нет по программе и что из материала ты собирался пропустить. Дальше — занятия по темам с проверкой ответов и интервальными повторениями.

Работает офлайн. Внешние модели опциональны и выключаются целиком.

## Запуск

Нужны Node 22+ и Python 3.13+.

```bash
npm install && cd frontend && npm install && cd ../backend && pip install -r requirements-dev.txt
```

Дальше из корня:

```bash
npm run dev
```

Фронтенд — http://localhost:5173, API — http://localhost:8000, документация API — http://localhost:8000/docs.

Либо в контейнерах:

```bash
docker compose up
```

## Проверка

```bash
npm run typecheck && npm run build
python backend/scripts/check_stage2.py
```

Тестового фреймворка пока нет: оснастка появится на этапе 5, когда будет что тестировать. Сквозная проверка этапа 2 поднимает чистую базу, переживает перезапуски и проверяет создание черновика и проекта.

## Что где

| Каталог | Что |
| --- | --- |
| `backend/` | FastAPI |
| `frontend/` | Vite + React + TypeScript |
| `data/` | SQLite и файловое хранилище. В git не едет |
| `.agents/skills/` | Проектные скиллы для агентов |
| `virtex/` | Прошлый проект, откуда взят UI-кит. Справочник, в git не едет |

Документы проекта: `BRIEF.md` — о продукте, `REQUIREMENTS.md` — требования, `PLAN.md` — план работ, `AGENTS.md` — правила для агентов, `docs/architecture/stage-2-core.md` — реализованное ядро данных и API.

## Состояние

Этап 2 завершён: локальная база и API уже сохраняют черновик мастера, паспорт цели, программу проекта и раскладку Рабочей области. Следующий шаг — этап 3: подключить к этому ядру существующие экраны мастера, Проектов, Программы и Рабочей области вместо демонстрационных данных.
