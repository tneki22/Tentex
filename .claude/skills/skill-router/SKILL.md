---
name: skill-router
description: Выбрать минимальный набор скиллов перед нетривиальной работой над Tentex. Использовать в начале многошаговой задачи, когда подходит сразу несколько скиллов, когда пользователь спрашивает «каким скиллом это делать», или когда важно не навешать лишних процессов. Не использовать для коротких ответов и одиночных очевидных команд.
---

# Выбор скилла

Бери минимум, который улучшит результат. Обычно это **один рабочий скилл плюс один проверяющий**. Не складывай процессные скиллы друг на друга, если задача не крупная и не рискованная.

Если задача маленькая и очевидная — не бери ничего и просто сделай.

## Сначала посмотри

1. `AGENTS.md` в корне — правила проекта, команды, границы этапов.
2. `PLAN.md` — на каком этапе мы сейчас и что в него входит.
3. `REQUIREMENTS.md` — если вопрос про поведение продукта, ответ скорее всего уже там.

Проектные скиллы важнее глобальных.

## Таблица

| Ситуация | Скилл |
| --- | --- |
| Проектируем экран на этапе 1 | `tentex-screens` |
| Нужен компонент интерфейса — есть ли готовый | `tentex-ui-kit` |
| Вопрос по библиотеке, API, конфигу, версии | `find-docs` |
| Баг, падающий тест, необъяснимое поведение | `systematic-debugging` |
| Собираемся сказать «готово» | `verification-before-completion` |
| Большая многошаговая задача без плана | `writing-plans` |
| Есть написанный план, надо его выполнить | `executing-plans` |
| Проверить идею на прочность до того, как кодить | `grilling` |
| Кончается контекст, работа не закончена | `handoff` |
| Функциональные сценарии в браузере (этап 11) | `playwright` |
| Внешний вид, вёрстка, визуальные решения | `frontend-design` |
| Термины предметной области, ADR | `domain-modeling` |
| Границы модулей, где резать интерфейс | `codebase-design` |
| Записка в Word, .docx | `anthropic-skills:docx` |
| Что-то из PDF (методичка, учебники) | `anthropic-skills:pdf` |
| Графики и диаграммы с данными | `dataviz` |
| Ревью незакоммиченных изменений | `/code-review` |

## Правила

- Уточняющий вопрос — не больше одного, и только если неверная догадка дорого обойдётся.
- Проверка после реализации лучше обязательного test-first. TDD в этом проекте выключен сознательно.
- Тесты писать там, где поведение легко ломается: разбиение на блоки, интервалы SM-2, нормализация ответов, сравнение результатов SQL.
- Планирование — только когда работа задевает несколько файлов или решений. Одноразовый скрипт разведки планировать не надо.
- `grilling` — когда непонятна сама постановка, а не как обязательный шаг перед каждой задачей.

## Что сказать

Одна строка, если это неочевидно:

`Беру: <скилл-а>, <скилл-б>. Остальное не нужно, потому что <причина>.`

Дальше сразу делай.
